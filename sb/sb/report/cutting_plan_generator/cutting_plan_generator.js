// Filters are defined in the report JSON file (cutting_plan_generator.json)
// No need to define them here to avoid duplicates
frappe.query_reports["Cutting Plan Generator"] = {
    // Filters are already defined in the report JSON file
}